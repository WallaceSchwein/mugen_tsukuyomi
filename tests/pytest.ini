[pytest]
markers =    
    integration : run all integration tests
    unit : run all unit tests
    preprocessing : run all unit tests, to test the preprocessing
    feature_generation : run all unit tests, to test the feature_generation